<%= form_tag(models_path, method: :get) do %>
  <%= select_tag "webmotors_make_id", options_for_select(make_options) %>
  <%= submit_tag "Buscar modelos", data: { disable_with: "Buscando..." } %>
<% end %>
